# Core Dependencies
pydantic>=2.0.0
pyyaml>=6.0
python-dotenv>=1.0.0

# AI APIs
anthropic>=0.18.0
openai>=1.0.0
elevenlabs>=0.2.26  # convert_with_timestamps() サポート (2024年5月以降)
requests>=2.31.0    # Kokoro TTS FastAPI との通信に使用

# YouTube API (Phase 9)
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1

# Video Processing
moviepy>=1.0.3
opencv-python>=4.8.0
pillow>=10.0.0

# Audio Processing
openai-whisper>=20231117   # 音声認識とタイミング情報取得用
stable-ts>=2.17.0          # Whisperの拡張版：無音抑制、ギャップ調整、タイミング精度向上
mecab-python3>=1.0.0       # 字幕生成での形態素解析に使用
ipadic>=1.0.0             # MeCab用標準辞書データ

# DTW Alignment (高精度音声-字幕同期)
fastdtw==0.3.4            # Dynamic Time Warping高速実装
scipy>=1.11.0             # 科学計算ライブラリ（DTWの距離計算に使用）

# Debugging & Visualization (オプション)
matplotlib>=3.7.0         # データ可視化（DTWアライメントのヒートマップ用）
seaborn>=0.12.0           # 統計的データ可視化（デバッグ用）

# CLI & Logging
rich>=13.0.0
click>=8.1.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Development Tools
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0